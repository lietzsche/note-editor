{
  "name": "note-editor-v2",
  "version": "2.0.0-alpha.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run build && npx wrangler dev --port 8788",
    "dev:watch": "concurrently \"vite build --watch\" \"npx wrangler dev --port 8788\"",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && npx wrangler deploy",
    "deploy:staging": "npm run build && npx wrangler deploy --env staging",
    "deploy:production": "npm run build && npx wrangler deploy --env production",
    
    "db:migrate:local": "npx wrangler d1 migrations apply note-editor-v2-db --local",
    "db:migrate:remote": "npx wrangler d1 migrations apply note-editor-v2-db --remote",
    "db:create-migration": "npx wrangler d1 migrations create note-editor-v2-db",
    "db:list:local": "npx wrangler d1 migrations list note-editor-v2-db --local",
    "db:list:remote": "npx wrangler d1 migrations list note-editor-v2-db --remote",
    "db:backup": "npx wrangler d1 backup create note-editor-v2-db",
    "db:restore": "npx wrangler d1 backup restore note-editor-v2-db",
    
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext ts,tsx --report-unused-disable-directives --max-warnings 0",
    "lint:fix": "eslint src --ext ts,tsx --fix",
    "format": "prettier --write \"src/**/*.{ts,tsx,css,md}\"",
    
    "test:unit": "vitest run --config vitest.unit.config.ts",
    "test:integration": "npm run build && vitest run --config vitest.config.ts",
    "test": "npm run test:unit && npm run test:integration",
    "test:watch": "vitest --config vitest.unit.config.ts",
    "test:integration:watch": "vitest --config vitest.config.ts",
    "test:coverage": "vitest run --coverage",
    
    "verify": "npm run typecheck && npm run lint && npm test",
    "prepare": "husky"
  },
  "dependencies": {
    "@dnd-kit/core": "^6.3.1",
    "@dnd-kit/sortable": "^10.0.0",
    "@dnd-kit/utilities": "^3.2.2",
    "hono": "^4.6.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.26.2"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.0",
    "@cloudflare/workers-types": "^4.20241112.0",
    "@types/react": "^18.3.11",
    "@types/react-dom": "^18.3.1",
    "@typescript-eslint/eslint-plugin": "^7.13.1",
    "@typescript-eslint/parser": "^7.13.1",
    "@vitejs/plugin-react": "^4.3.2",
    "concurrently": "^9.1.0",
    "eslint": "^8.57.0",
    "eslint-plugin-react-hooks": "^4.6.2",
    "eslint-plugin-react-refresh": "^0.4.7",
    "husky": "^9.0.11",
    "prettier": "^3.2.5",
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vitest": "^2.0.0",
    "wrangler": "^4.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}