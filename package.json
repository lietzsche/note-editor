{
  "name": "note-editor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run build && npx wrangler pages dev build/ --d1=DB --compatibility-date=2024-01-01",
    "dev:watch": "concurrently \"vite build --watch\" \"npx wrangler pages dev build/ --d1=DB --compatibility-date=2024-01-01\"",
    "build": "vite build",
    "deploy": "npm run build && npx wrangler pages deploy build/ --project-name note-editor-app",
    "migrate:create": "npx wrangler d1 migrations create DB",
    "migrate:apply:local": "npx wrangler d1 migrations apply DB --local",
    "migrate:apply:remote": "npx wrangler d1 migrations apply DB --remote",
    "migrate:list:local": "npx wrangler d1 migrations list DB --local",
    "migrate:list:remote": "npx wrangler d1 migrations list DB --remote",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.26.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241112.0",
    "@types/react": "^18.3.11",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.2",
    "concurrently": "^9.1.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "wrangler": "^3.87.0"
  }
}
