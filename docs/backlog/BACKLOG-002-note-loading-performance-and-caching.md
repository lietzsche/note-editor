# BACKLOG-002: 노트/그룹 전환 체감 속도 개선 및 캐싱 검토

- 문서 버전: v0.1
- 작성일: 2026-04-15
- 상태: Backlog
- 우선순위: High
- 성격: 성능/UX 개선
- 기준 문서: `docs/testing/backlog/TEST-BACKLOG-002-note-loading-performance-and-caching.md`

## 1. 문제 정의

현재 프로젝트는 노트나 그룹을 선택할 때 체감 지연이 발생한다는 사용성 문제가 있다.

특히 다음 흐름이 느리게 느껴질 수 있다.

1. 그룹 클릭 후 해당 그룹 노트가 보이기까지의 시간
2. 노트 클릭 후 편집기 내용이 안정적으로 보이기까지의 시간
3. 생성/삭제/이동 이후 목록이 다시 반영되기까지의 시간

## 2. 현재 구현에서 확인된 원인 후보

현재 코드 기준으로 다음 특성이 확인된다.

1. 그룹 선택 시마다 `loadNotes(selectedGroupId)`가 다시 호출되어 네트워크 재조회가 발생한다.
2. 노트 목록 조회 API가 목록용 요약이 아니라 `content`를 포함한 전체 노트 데이터를 내려준다.
3. 생성/삭제/그룹 이동 후에도 전체 목록을 다시 조회하는 흐름이 많다.
4. 그룹/노트/에디터 상태가 `NotesPage`에 집중되어 있어 큰 상태 변경 시 재렌더 부담이 커질 수 있다.
5. 그룹/노트 목록에 대한 메모리 캐시, stale-while-revalidate, 요청 중복 제거가 없다.

## 3. 검토 목표

1. 반복 방문하는 그룹과 노트는 더 빠르게 보여야 한다.
2. 캐시를 도입하더라도 편집 무결성과 최신성 정책이 깨지면 안 된다.
3. 사용자에게는 빈 화면보다 `즉시 표시 + 백그라운드 갱신`이 우선되어야 한다.
4. 저위험 개선부터 단계적으로 적용할 수 있어야 한다.

## 4. 검토 가능한 대응안

### 4.1 프론트엔드 메모리 캐시

1. 그룹 목록 캐시
2. 그룹별 노트 목록 캐시
3. 노트 상세 캐시

장점:

1. 구현 리스크가 비교적 낮다.
2. 같은 세션 내 재방문 속도를 빠르게 만들 수 있다.

주의점:

1. 생성/수정/삭제/이동 후 캐시 무효화 정책이 필요하다.

### 4.2 Stale-While-Revalidate 패턴

1. 캐시된 데이터가 있으면 먼저 즉시 렌더링
2. 백그라운드에서 최신 데이터를 다시 조회
3. 차이가 있으면 조용히 상태 갱신

장점:

1. 그룹 재선택 시 체감 속도 개선 효과가 크다.
2. 완전한 오프라인 캐시보다 구현 난도가 낮다.

### 4.3 목록/상세 API 분리

1. 목록 API는 `id`, `title`, `group_id`, `sort_order`, `updated_at`, 선택적 snippet만 반환
2. 본문 `content`는 상세 조회 시 불러오거나 미리 예열(prefetch)한다.

장점:

1. 그룹 전환 시 payload를 크게 줄일 수 있다.
2. 많은 노트/긴 본문 환경에서 효과가 크다.

주의점:

1. 노트 클릭 시 detail fetch가 추가되면 오히려 느려질 수 있다.
2. 따라서 상세 캐시 또는 prefetch와 함께 검토해야 한다.

### 4.4 낙관적 목록 업데이트

1. 생성/삭제/이동/정렬 후 전체 재조회 전에 로컬 상태를 먼저 갱신
2. 서버 응답 실패 시 롤백

장점:

1. 조작 직후 반응성이 좋아진다.

### 4.5 계측 및 성능 기준 수립

1. 그룹 전환 시간
2. 노트 열기 시간
3. 캐시 hit/miss 비율
4. 노트 수/본문 크기에 따른 payload 크기

장점:

1. 체감 이슈를 정량화하고 우선순위를 잡을 수 있다.

## 5. 권장 추진 순서

### Task 1. 계측 및 병목 확인

주요 작업:

1. 프론트엔드에서 그룹 전환/노트 열기 타이밍 측정
2. API 응답 크기와 응답 시간 기록
3. 빈 화면, 스피너, 렌더 지연이 각각 어느 구간에서 발생하는지 분리

완료 기준:

1. 느림의 주원인이 네트워크인지 렌더링인지 구분됨
2. 개선 전 기준선이 기록됨

### Task 2. 저위험 캐시 도입

주요 작업:

1. 그룹 목록과 그룹별 노트 목록 메모리 캐시 도입
2. 그룹 재선택 시 캐시 우선 렌더 + 백그라운드 갱신
3. 생성/삭제/이동 이후 캐시 무효화 또는 부분 갱신

완료 기준:

1. 같은 세션 내 반복 그룹 전환 속도 개선
2. 데이터 최신성 오류 없이 캐시 동작

### Task 3. 목록 payload 경량화 검토

주요 작업:

1. 목록 API에 `content` 포함 유지 여부 판단
2. 목록/상세 API 분리 시뮬레이션
3. 필요 시 노트 상세 캐시 및 prefetch 설계

완료 기준:

1. 요약 목록 + 상세 캐시 모델 채택 여부 확정
2. 대량 노트 환경에서 payload 절감 방향 결정

### Task 4. UI 구조 및 상태 최적화

주요 작업:

1. `NotesPage` 상태 분리 검토
2. 목록/편집 패널 렌더링 분리
3. 불필요한 전체 리렌더 감소

완료 기준:

1. 노트 선택 시 화면 전환이 더 가볍게 동작
2. 구조 개선이 회귀 없이 유지됨

## 6. 수용 기준

1. 같은 세션에서 이미 본 그룹은 재선택 시 더 빠르게 보여야 한다.
2. 노트 선택 시 빈 본문이나 늦은 본문 치환 같은 어색한 지연이 줄어들어야 한다.
3. 편집 중인 노트의 최신성/충돌 정책이 캐시 때문에 깨지지 않아야 한다.
4. 생성/수정/삭제/이동 후 오래된 캐시가 잘못 남지 않아야 한다.
5. 개선 전/후 비교가 가능한 성능 기록이 남아야 한다.

## 7. 예상 영향 파일

- `src/pages/NotesPage.tsx`
- `src/lib/api.ts`
- `functions/api/[[path]].ts`
- `tests/api/auth.test.ts`
- `docs/testing/backlog/TEST-BACKLOG-002-note-loading-performance-and-caching.md`

분리 후보:

- `src/lib/cache.ts`
- `src/lib/noteQueries.ts`
- `src/components/NotesLayout.tsx`

## 8. 오픈 이슈

1. 캐시를 메모리만 사용할지, `sessionStorage`/`IndexedDB`까지 확장할지
2. 목록에서 `content`를 제거할 경우 노트 열기 체감 속도를 어떻게 보완할지
3. 성능 개선의 1차 목표를 체감 기준으로 둘지, 수치 SLA로 둘지
